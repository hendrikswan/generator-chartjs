language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chartjs' ]; then cd .. && eval "mv $currentfolder generator-chartjs" && cd generator-chartjs; fi

